meta {
  name: CSV generador
  type: http
  seq: 10
}

get {
  url: {{base_url}}/api/orders/export
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token_admin}}
}

settings {
  encodeUrl: true
}
